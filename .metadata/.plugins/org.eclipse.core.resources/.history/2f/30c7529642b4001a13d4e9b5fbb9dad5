package SWexpert;
import java.util.*;
public class Swea3376 {
	static Scanner sc = new Scanner(System.in);
	static int C;
	static int[] wave;
	static int N;
	public static void main(String[] args) {
		C = sc.nextInt();
		
		for(int tc=1;tc<=C;tc++) {
			N = sc.nextInt();
			
			wave = new int[N];
			
			if(N >=5) {
				wave[0] = 1;
				wave[1] = 1;
				wave[2] = 1;
				wave[3] = 2;
				wave[4] = 2;
				
				int result = find(N);
				System.out.println("#" + tc + " " + result);
			}
			
		}

	}
	private static int find(int n) {
		
		if(n<=5) {
			return wave[n-1];
		}else {
			return find(n-1) + find(n-5);
		}
	}

}
